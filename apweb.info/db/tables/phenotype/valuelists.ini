[subclass_list]
__sql__ = "SELECT subclass.id, CONCAT(subclass.name, ' (', class.name ,')') FROM subclass, class WHERE subclass.classID = class.id ORDER BY subclass.name"

[taxa_list]
__sql__ = "SELECT id, name FROM taxa ORDER BY name"
